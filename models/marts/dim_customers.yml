version: 2

models:

  - name: dim_customers
    description: Tabela com os dados dos clientes.
    columns:
      - name: customer_id
        description: Chaves primárias com os id's dos clientes.
        tests:
          - not_null
          - unique 
      - name: name_customer
        description: Nomes dos clientes cadastrados.
        tests:
          - not_null